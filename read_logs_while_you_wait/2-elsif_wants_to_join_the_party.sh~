#!/bin/bash

#Write a script that takes a file path as an argument and output the content of this file. Goal: to get some numbers about the traffic that this web server is getting. No cat command. Use while loop. Script must count the number of times we got HEAD and GET requests. First line of your script output must show number of HEAD. Second ine of your script output must show number of GET.

while true; do
    elsif echo
    then
       count "HEAD" $1 && count "GET" $1
    else
	echo 
    fi
done
